함수 함수명()
	모양: 이름뒤에 소괄호
	기능: 어떠한 기능을 한다
		->연산자랑 똑같다..! 
	-> 입력값타입, 함수사용 결과, 함수사용 결과타입이 중요하다.
	함수의 피연산자는(입력값) 괄호 안에쓴다.

<1>숫자타입 함수 : 숫자를 입력받아 숫자를 반환한다.
<2>문자타입 함수 : 문자를 입력받아 문자 혹은 숫자를 반환한다.
			->괄호 안에는 직접적인 문자열 값이 와도되고, 문자열 데이터가 존재하는 열 이름이 와도 된다.